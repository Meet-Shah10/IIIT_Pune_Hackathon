// TODO: Phase 1 — Memory.js Mongoose schema
// { _id, userId, content, category, sensitivity, retention, expiresAt, status, sourceMessageId, createdAt, updatedAt }
// status: "active" | "expired" | "forgotten"
// retention: "permanent" | "temporary" | "session" | "declined"
